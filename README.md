cat > README.md << 'EOF'

# Operating Systems Project

This repository contains an operating systems project implementing core OS components such as:

- Filesystem (`filesys`)
- Thread library (`threadlib`)
- Virtual memory pager (`vmpager`)
- Test applications (`testapp`)

## Build

Build instructions depend on the provided Makefiles in each module:

```bash
make
```
